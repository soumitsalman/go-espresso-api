name: Trigger auto deployment for cafecito-espresso-api

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/cafecito-espresso-api-AutoDeployTrigger-77af90e6-b636-49f9-96ad-0d339e244a75.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CAFECITOESPRESSOAPI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CAFECITOESPRESSOAPI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CAFECITOESPRESSOAPI_AZURE_SUBSCRIPTION_ID }}

      - name: Render containerapp YAML
        run: |
            mkdir -p .github/containerapp
            cat > .github/containerapp/containerapp.rendered.yaml <<EOF
            properties:
              template:
                containers:
                  - name: main
                    image: docker.io/soumitsr/espresso-api:${GITHUB_SHA}
                    resources:
                      cpu: 0.5
                      memory: 1Gi
                    env:
                    - name: PG_CONNECTION_STRING
                      secretRef: pg-connection-string
                    - name: API_KEYS
                      secretRef: api-keys
                    - name: MAX_CONCURRENT_REQUESTS
                      value: '512'
                    - name: EMBEDDER_BASE_URL
                      value: localhost:10000
                  - name: embedder
                    image: docker.io/soumitsr/tei-static:latest
                    resources:
                      cpu: 0.5
                      memory: 1Gi
            EOF

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: docker.io
          registryUsername: ${{ secrets.CAFECITOESPRESSOAPI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CAFECITOESPRESSOAPI_REGISTRY_PASSWORD }}
          containerAppName: cafecito-espresso-api
          resourceGroup: frontends-rg
          imageToBuild: soumitsr/espresso-api:${{ github.sha }}
          yamlConfigPath: .github/containerapp/containerapp.rendered.yaml
          _buildArgumentsKey_: |
            _buildArgumentsValues_


